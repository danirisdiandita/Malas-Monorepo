$schema: "https://moonrepo.dev/schemas/project.json"
language: "javascript"

tasks:
  dev:
    command: "./node_modules/.bin/expo start --android"
    options:
      persistent: true

  android:
    command: "./node_modules/.bin/expo run:android"
