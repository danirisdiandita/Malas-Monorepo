// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/danirisdiandita/malas-monorepo/api/ent/revenuecatidentity"
	"github.com/danirisdiandita/malas-monorepo/api/ent/user"
)

// RevenueCatIdentity is the model entity for the RevenueCatIdentity schema.
type RevenueCatIdentity struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// RcAppUserID holds the value of the "rc_app_user_id" field.
	RcAppUserID string `json:"rc_app_user_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RevenueCatIdentityQuery when eager-loading is set.
	Edges        RevenueCatIdentityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RevenueCatIdentityEdges holds the relations/edges for other nodes in the graph.
type RevenueCatIdentityEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RevenueCatIdentityEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RevenueCatIdentity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case revenuecatidentity.FieldID, revenuecatidentity.FieldUserID:
			values[i] = new(sql.NullInt64)
		case revenuecatidentity.FieldRcAppUserID:
			values[i] = new(sql.NullString)
		case revenuecatidentity.FieldCreatedAt, revenuecatidentity.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RevenueCatIdentity fields.
func (_m *RevenueCatIdentity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case revenuecatidentity.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case revenuecatidentity.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = int(value.Int64)
			}
		case revenuecatidentity.FieldRcAppUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rc_app_user_id", values[i])
			} else if value.Valid {
				_m.RcAppUserID = value.String
			}
		case revenuecatidentity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case revenuecatidentity.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RevenueCatIdentity.
// This includes values selected through modifiers, order, etc.
func (_m *RevenueCatIdentity) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the RevenueCatIdentity entity.
func (_m *RevenueCatIdentity) QueryUser() *UserQuery {
	return NewRevenueCatIdentityClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this RevenueCatIdentity.
// Note that you need to call RevenueCatIdentity.Unwrap() before calling this method if this RevenueCatIdentity
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RevenueCatIdentity) Update() *RevenueCatIdentityUpdateOne {
	return NewRevenueCatIdentityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RevenueCatIdentity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RevenueCatIdentity) Unwrap() *RevenueCatIdentity {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RevenueCatIdentity is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RevenueCatIdentity) String() string {
	var builder strings.Builder
	builder.WriteString("RevenueCatIdentity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("rc_app_user_id=")
	builder.WriteString(_m.RcAppUserID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RevenueCatIdentities is a parsable slice of RevenueCatIdentity.
type RevenueCatIdentities []*RevenueCatIdentity
