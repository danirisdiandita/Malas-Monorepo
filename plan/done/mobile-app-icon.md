# Replace the mobile app icon

Replace this single Expo icon asset manually:

- `mobile/assets/images/icon.png` — main 1024×1024 app icon

Rebuild the Android app after changing the assets:

```bash
moon run mobile:android
```
